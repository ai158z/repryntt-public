# Bootstrap Templates

These files are copied to `~/.repryntt/brain/bootstrap/` on first run.
They provide the agent personality framework and operational protocols.

Do NOT edit these — edit the copies in your data directory instead.
