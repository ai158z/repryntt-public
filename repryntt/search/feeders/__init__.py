"""
repryntt.search.feeders — Data feeder modules.

Each feeder produces structured stimulus data that gets aggregated into
data/aggregated_stimulus.json, read by the heartbeat loop every tick.

Migration source: SAIGE/feeders/
"""
